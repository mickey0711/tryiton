{
    "name": "@tryiton/web",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "next dev -p 3000",
        "build": "if [ -d '../../services/api/public/_next' ]; then mkdir -p out && cp -r ../../services/api/public/. out/ && echo 'Used pre-built assets from services/api/public'; else next build; fi",
        "start": "next start"
    },
    "dependencies": {
        "next": "^14.2.5",
        "react": "^18.3.1",
        "react-dom": "^18.3.1"
    },
    "devDependencies": {
        "@types/node": "^20.14.12",
        "@types/react": "^18.3.3",
        "@types/react-dom": "^18.3.0",
        "typescript": "^5.4.5"
    }
}